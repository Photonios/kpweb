import React from 'react';
import ReactDOM from 'react-dom';
import { App } from '@kpweb/app';

window.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<App />, document.getElementById('app'));
});
